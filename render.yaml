services:
  - type: web
    name: rentmatch
    env: native
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: OPENCAGE_API_KEY
        sync: false